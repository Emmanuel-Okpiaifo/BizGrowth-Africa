# Copy to .env and fill in your keys. Do not commit .env.

# Google Sheets (for articles, opportunities, tenders)
# Used by the PHP API locally when .htaccess is not in use. On cPanel, set these in .htaccess SetEnv instead.
VITE_GOOGLE_SHEETS_API_KEY=
VITE_GOOGLE_SHEETS_ID=
VITE_GOOGLE_APPS_SCRIPT_URL=

# Optional: same names without VITE_ prefix (API reads both)
# GOOGLE_SHEETS_API_KEY=
# GOOGLE_SHEETS_ID=
# GOOGLE_APPS_SCRIPT_URL=